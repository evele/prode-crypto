import data from '../../helpers/data';

const HeroSection = () => {
  return (
    <section className="hero">
      <title>{data.info}</title>
      {/* Subtitle */}
      {/* CTA button */}
    </section>
  );
};

export default HeroSection;